# R Adapter Code

R backend adapter for nlmixr2 subprocess execution.
`harness.R` reads a JSON request, runs nlmixr2, and writes a JSON response for the
Python subprocess runner. `impute.R` provides the R-backed multiple-imputation helpers.
