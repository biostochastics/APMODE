{
  "raise_typer_exit_count": 62,
  "rationale": "Locked by scripts/check_typer_exit_count.py. PR2 introduced typed-error infrastructure at apmode.cli_errors + an entrypoint catcher at apmode.__main__. Subsequent PRs migrate individual call sites and lower this count. Run `python scripts/check_typer_exit_count.py --update` after a migration. Bumped 58 -> 60: QA/QC remediation added `apmode run --provenance` with two new file-existence/parse-validation guards in the existing untyped raise-typer.Exit style used throughout `run()`. Bumped 60 -> 62: QA/QC remediation also added `apmode run --model-influence`/`--decision-consequence` (V&V40 risk-grading) with two new value-validation guards in the same style; no cli_errors migration is in flight for this command, so all four are intentional additions, not a regression."
}
